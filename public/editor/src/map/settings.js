export const mapSettings = {
    size: {
        width: 2000,
        height: 2000,
    },
    coord: {
        current: null,
    },
};

export const matrix = [];

export const matrix_collision = [];

export let drawmatrix = {
    value: null
};

map.style.width = mapSettings.size.width + 'px';
map.style.height = mapSettings.size.height + 'px';
